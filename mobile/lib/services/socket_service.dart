// lib/services/socket_service.dart
// Stub — real WebSocket is managed by MessagingRepository using
// web_socket_channel (works on Android, iOS, and Web).
// Kept so existing imports don't break.
class SocketService {
  SocketService._();
}